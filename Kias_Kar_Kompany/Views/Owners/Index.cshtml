@model IEnumerable<Kias_Kar_Kompany.Models.Owner>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>

            <th>
                @* @Html.DisplayNameFor(model => model.ownerName) *@
                owner Name
            </th>
            <th>
                @* @Html.DisplayNameFor(model => model.ownerEmail) *@
                owner Email
            </th>
            <th>
                @* @Html.DisplayNameFor(model => model.ownerNumber) *@
                owner Number
            </th>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>

                <td>
                    @Html.DisplayFor(modelItem => item.ownerName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ownerEmail)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ownerNumber)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ownerId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.ownerId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.ownerId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
